name: platform_adaptive_buttons
description: "A Flutter package that provides a custom button that adapts to the platform's design guidelines."
version: 0.0.1
repository: https://github.com/YoussefLasheen/platform_adaptive_buttons
issue_tracker: https://github.com/YoussefLasheen/platform_adaptive_buttons/issues

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
